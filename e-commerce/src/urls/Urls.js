// Les URLs de ce projet

// Route commune :
const commonPath = "http://localhost:8090"

// Articles :
export const URL_ADD = `${commonPath}/api/article/add` // url pour ajouter un article => POST

// Users :
export const URL_SIGNUP = `${commonPath}/api/user/signup` // url pour se connecter => POST
export const URL_SIGN = `${commonPath}/api/user/sign` // url pour se connecter => POST